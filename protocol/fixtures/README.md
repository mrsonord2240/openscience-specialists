# Protocol fixtures

Everything in this directory is test-only and non-publishable. The committed Ed25519 private key is
deliberately public and must never be accepted by the production publication workflow.

Valid fixtures cover optional author discovery metadata and an ordinary package attachment. The
attachment remains outside Skill content while the full archive still passes the same ZIP safety
scan.
